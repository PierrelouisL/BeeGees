#include "bees.h"

void testGPS(){
  //Serial.println("Hello GPS!");
}

/*unsigned char buffer_t[64];                   // buffer array for data receive over serial port
int count=0;                                // counter for buffer array

void loop()
{
  Serial.println("test");
    if (Serial1)                     // if date is coming from software serial port ==> data is coming from SoftSerial shield
    {
      Serial.println("Test");
      Serial.print(Serial1.readString());
        //while(Serial1)               // reading data into char array
        //{
          
            //buffer_t[count++]=Serial1.read();      // writing data into array
            //Serial.print(Serial1.readString());
            //if(count == 64)break;
            //Serial.print("t");
        //}
        //Serial.print(Serial1.readString());
        //Serial.write(buffer_t,count);                 // if no data transmission ends, write buffer to hardware serial port
        //clearBufferArray();                         // call clearBufferArray function to clear the stored data from the array
        //count = 0;                                  // set counter of while loop to zero 
    //}
    if (Serial.available())                 // if data is available on hardware serial port ==> data is coming from PC or notebook
    Serial1.write(Serial.read());        // write it to the SoftSerial shield
}*/
 
 
/*void clearBufferArray()                     // function to clear buffer array
{
    for (int i=0; i<count;i++)
    {
        buffer_t[i]=NULL;
    }                      // clear all index of array with command NULL
}*/
